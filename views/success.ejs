<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>授权成功 - Copilot OpenAI Proxy</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  <div class="container mx-auto px-4 py-8 max-w-2xl">
    <!-- Success Icon -->
    <div class="text-center mb-8">
      <svg class="w-24 h-24 mx-auto text-green-500 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
      </svg>
      <h1 class="text-4xl font-bold text-gray-900 mb-2">✅ 授权成功！</h1>
      <p class="text-gray-600">您现在可以使用 Copilot OpenAI Proxy 了</p>
    </div>

    <!-- User Info Card -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-6">
      <h2 class="text-2xl font-semibold mb-4">账户信息</h2>
      
      <div class="space-y-3">
        <div class="flex items-center justify-between py-2 border-b">
          <span class="text-gray-600">GitHub 用户名</span>
          <span class="font-semibold"><%= status.user?.login || 'Unknown' %></span>
        </div>
        
        <% if (status.user?.name) { %>
        <div class="flex items-center justify-between py-2 border-b">
          <span class="text-gray-600">姓名</span>
          <span class="font-semibold"><%= status.user.name %></span>
        </div>
        <% } %>
        
        <% if (status.expiresAt) { %>
        <div class="flex items-center justify-between py-2 border-b">
          <span class="text-gray-600">Token 有效期至</span>
          <span class="font-semibold"><%= new Date(status.expiresAt).toLocaleString('zh-CN') %></span>
        </div>
        <% } %>
        
        <div class="flex items-center justify-between py-2">
          <span class="text-gray-600">状态</span>
          <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800">
            <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            已激活
          </span>
        </div>
      </div>
    </div>

    <!-- Quick Actions -->
    <div class="bg-white rounded-lg shadow-md p-6 mb-6">
      <h2 class="text-2xl font-semibold mb-4">快速操作</h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
        <button onclick="testAPI()" class="bg-blue-500 hover:bg-blue-600 text-white px-6 py-3 rounded-lg font-medium transition flex items-center justify-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 10l-2 1m0 0l-2-1m2 1v2.5M20 7l-2 1m2-1l-2-1m2 1v2.5M14 4l-2-1-2 1M4 7l2-1M4 7l2 1M4 7v2.5M12 21l-2-1m2 1l2-1m-2 1v-2.5M6 18l-2-1v-2.5M18 18l2-1v-2.5"></path>
          </svg>
          测试 API
        </button>
        
        <a href="/" class="bg-gray-700 hover:bg-gray-800 text-white px-6 py-3 rounded-lg font-medium transition flex items-center justify-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
          </svg>
          返回首页
        </a>
      </div>
    </div>

    <!-- API Usage Guide -->
    <div class="bg-white rounded-lg shadow-md p-6">
      <h2 class="text-2xl font-semibold mb-4">开始使用</h2>
      
      <div class="mb-4">
        <p class="text-gray-700 mb-3">现在您可以通过以下 API 端点使用 Copilot：</p>
        
        <div class="bg-gray-50 p-4 rounded-lg border border-gray-200 mb-4">
          <p class="text-sm font-medium text-gray-700 mb-2">Chat Completions API:</p>
          <code class="block text-sm text-gray-800">POST http://localhost:3000/v1/chat/completions</code>
        </div>

        <div class="bg-blue-50 p-4 rounded-lg border border-blue-200">
          <p class="text-sm font-medium text-blue-900 mb-2">💡 提示：</p>
          <p class="text-sm text-blue-800">这是一个完全兼容 OpenAI API 的端点，您可以在任何支持 OpenAI API 的应用中使用它。</p>
        </div>
      </div>

      <div class="mt-6">
        <p class="text-sm font-medium text-gray-700 mb-2">示例代码：</p>
        <pre class="bg-gray-900 text-gray-100 p-4 rounded-lg overflow-x-auto text-sm"><code>curl http://localhost:3000/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-4o",
    "messages": [
      {"role": "user", "content": "Hello!"}
    ]
  }'</code></pre>
      </div>
    </div>
  </div>

  <script>
    async function testAPI() {
      const btn = event.target
      const originalText = btn.innerHTML
      btn.disabled = true
      btn.innerHTML = '<span class="inline-block animate-spin mr-2">⏳</span> 测试中...'

      try {
        const response = await fetch('/api/test', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' }
        })
        const data = await response.json()
        
        if (data.choices && data.choices[0]) {
          alert('✅ 测试成功！\n\n模型响应: ' + data.choices[0].message.content)
        } else {
          alert('❌ 测试失败\n\n' + JSON.stringify(data, null, 2))
        }
      } catch (error) {
        alert('❌ 测试失败\n\n' + error.message)
      } finally {
        btn.disabled = false
        btn.innerHTML = originalText
      }
    }
  </script>
</body>
</html>
